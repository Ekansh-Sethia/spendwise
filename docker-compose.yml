
services:
  spendwise-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spendwise
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - backend/.env
